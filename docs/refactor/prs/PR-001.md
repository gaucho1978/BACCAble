**PR-001 — Macierz budowania i mierzalna baza**

Typ: **TOOL**. Model: GPT-5.6 Sol / medium. Budżet produkcji: **0 linii**.

Wymagane dokumenty: [kontrakt](../EXECUTION_PL.md), [decyzje](../DECISIONS_PL.md), [akceptacja](../ACCEPTANCE_PL.md).

**Warunki wejścia:** brak. Bez dodatkowej bramki wejściowej; wymagany odbiór sprzętowy opisano poniżej.

**Problem i oczekiwany rezultat.** Każde zadanie buduje role identycznym poleceniem i raportuje rzeczywisty wynik.

**Dozwolone pliki produkcyjne / narzędziowe.** `(nowy)` oznacza plik do utworzenia w tej karcie. Zmiana pliku wspólnego obejmuje tylko wskazaną odpowiedzialność.

- `scripts/refactor/build.py (nowy)`
- `scripts/refactor/build-matrix.json (nowy)`
- `scripts/refactor/test_build.py (nowy)`
- `.gitignore`

**Dokładny zakres pracy.**

- Skrypt Python 3/stdlib z CLI B4/BX z ACCEPTANCE_PL; CFLAGS przez środowisko, osobny absolutny BUILD_DIR dla każdej konfiguracji wraz z HAL/USB.
- Manifest: -DRELEASE_FLAVOR=<X>_FLAVOR -D<X>_FLAVOR=1, rozszerzenia DEBUG_MODE/LARGE_DISPLAY/ACT_AS_SCHIZZAFORTE_SERIAL_CONTROLLER. Utrwalić wersję kompilatora, bez instalowania innego.
- JSON: SHA, dirty, komendy, toolchain, wynik per rola, ścieżki ELF/BIN/HEX/MAP, text/data/bss. Ignorować .refactor-out/.

**Poza zakresem.** Zmiana firmware, Makefile, naprawa DEBUG i flashowanie.

**Kryteria akceptacji.** Wszystkie warunki wspólne oraz:

- [ ] PR-001-A1: B4 tworzy cztery odrębne komplety artefaktów.
- [ ] PR-001-A2: Nieznany profil/brak kompilatora daje niezero; nieoczekiwany błąd pojedynczej roli nie jest ukrywany.
- [ ] PR-001-A3: BX rozróżnia znane błędy C2/BH DEBUG od regresji; stały tekst wersji pozwala powtórzyć wynik.
- [ ] PR-001-A4: Test CLI z atrapą subprocess sprawdza argumenty, izolację i kody wyjścia.

**Testy i dowody.** B4, BX, D; python3 -m unittest discover -s scripts/refactor -p 'test_*.py'. H powstanie w PR-003. Brak wymaganego testu sprzętowego dla zakresu tej karty; nie zalicza to odbioru późniejszej integracji.

Dozwolone artefakty: `tests/host/test_pr001.c`, odpowiadające atrapy w `tests/host/stubs/pr001/`, dane w `tests/fixtures/pr001/` i wyłącznie rejestracja tego testu w `tests/host/Makefile`.  Raport: `docs/refactor/evidence/PR-001.md`; rejestr: tylko wiersz PR-001 w `STATUS_PL.md`.

**Merge, cofnięcie i przekazanie.** Scalić po review i spełnieniu kryteriów. Cofnięcie przez revert tej karty; nie zmienia formatu danych trwałych ani protokołu. Po merge wykonać kontrole SHA main zgodnie z kryteriami wspólnymi. Następna karta korzysta z tego rezultatu dopiero po statusie VERIFIED_MAIN.

