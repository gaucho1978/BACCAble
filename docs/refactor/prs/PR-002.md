**PR-002 — Zależności nagłówków i flag w Makefile**

Typ: **FIX**. Model: GPT-5.6 Sol / medium. Budżet produkcji: **250 linii**.

Wymagane dokumenty: [kontrakt](../EXECUTION_PL.md), [decyzje](../DECISIONS_PL.md), [akceptacja](../ACCEPTANCE_PL.md).

**Warunki wejścia:** [PR-001](PR-001.md). Bez dodatkowej bramki wejściowej; wymagany odbiór sprzętowy opisano poniżej.

**Problem i oczekiwany rezultat.** Build przyrostowy odpowiada czystemu także po zmianie roli.

**Dozwolone pliki produkcyjne / narzędziowe.** `(nowy)` oznacza plik do utworzenia w tej karcie. Zmiana pliku wspólnego obejmuje tylko wskazaną odpowiedzialność.

- `firmware/ledsStripController/Makefile`

**Dokładny zakres pracy.**

- Dodać -MMD -MP i include .d dla aplikacji, HAL/USB; źródło w regule to $<, nie lista wszystkich zależności $^.
- Stempel efektywnej konfiguracji w BUILD_DIR obejmuje compiler/CFLAGS/linker/ldflags i zmienia mtime tylko przy zmianie treści; unieważnia wszystkie obiekty.
- Clean usuwa wygenerowane pliki wybranego zwalidowanego BUILD_DIR, również biblioteki. Usunąć hardcoded mkdir build; nie naruszać innych katalogów.

**Poza zakresem.** CMake, nowe optymalizacje, nowe wersje bibliotek i nazwy obrazów.

**Kryteria akceptacji.** Wszystkie warunki wspólne oraz:

- [ ] PR-002-A1: Drugie make bez zmian nie wywołuje kompilatora ani linkera.
- [ ] PR-002-A2: Zmiana setup_menu.h/usbd_conf.h przebudowuje użytkowników.
- [ ] PR-002-A3: C1→BH w tym samym katalogu daje obraz zgodny z czystym BH przy stałym tekście wersji.
- [ ] PR-002-A4: Clean usuwa .o/.d/.a tej konfiguracji i pozostawia inne oraz źródła.

**Testy i dowody.** B4, BX, D, M; skryptowy test czterech opisanych przebiegów make z logiem. Brak wymaganego testu sprzętowego dla zakresu tej karty; nie zalicza to odbioru późniejszej integracji.

Dozwolone artefakty: `tests/host/test_pr002.c`, odpowiadające atrapy w `tests/host/stubs/pr002/`, dane w `tests/fixtures/pr002/` i wyłącznie rejestracja tego testu w `tests/host/Makefile`.  Raport: `docs/refactor/evidence/PR-002.md`; rejestr: tylko wiersz PR-002 w `STATUS_PL.md`.

**Merge, cofnięcie i przekazanie.** Scalić po review i spełnieniu kryteriów. Cofnięcie przez revert tej karty; nie zmienia formatu danych trwałych ani protokołu. Po merge wykonać kontrole SHA main zgodnie z kryteriami wspólnymi. Następna karta korzysta z tego rezultatu dopiero po statusie VERIFIED_MAIN.

