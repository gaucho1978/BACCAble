**PR-003 — Testy hosta pod sanitizerami w CI**

Typ: **TEST**. Model: GPT-5.6 Sol / medium. Budżet produkcji: **0 linii**.

Wymagane dokumenty: [kontrakt](../EXECUTION_PL.md), [decyzje](../DECISIONS_PL.md), [akceptacja](../ACCEPTANCE_PL.md).

**Warunki wejścia:** [PR-002](PR-002.md). Bez dodatkowej bramki wejściowej; wymagany odbiór sprzętowy opisano poniżej.

**Problem i oczekiwany rezultat.** Testy używają rzeczywistego produkcyjnego C, bez kopiowania funkcji i sprzętu.

**Dozwolone pliki produkcyjne / narzędziowe.** `(nowy)` oznacza plik do utworzenia w tej karcie. Zmiana pliku wspólnego obejmuje tylko wskazaną odpowiedzialność.

- `tests/host/Makefile (nowy)`
- `tests/host/test_support.h (nowy)`
- `tests/host/test_support.c (nowy)`
- `.github/workflows/ci.yml`

**Dokładny zakres pracy.**

- Mały runner C11, SANITIZE=1 i TEST=nazwa; ASan/UBSan przerywają proces. Bez zewnętrznego frameworka.
- Pierwszy test calculateCRC z wektorem referencyjnym i granicami, kompilujący functions_Common.c z minimalnymi atrapami oraz usuwaniem nieużywanych sekcji.
- Dodać host-tests do CI, zachować lint i cztery buildy.

**Poza zakresem.** Ekstrakcja całej aplikacji i czerwone testy pozostałych usterek.

**Kryteria akceptacji.** Wszystkie warunki wspólne oraz:

- [ ] PR-003-A1: H przechodzi; celowo złe oczekiwanie w kontrolowanej próbie daje niezero, następnie zostaje przywrócone.
- [ ] PR-003-A2: Nieznana nazwa testu i pusty zestaw dają błąd.
- [ ] PR-003-A3: Test linkuje produkcyjny calculateCRC; niezależny wektor i znaczniki bufora są sprawdzane.
- [ ] PR-003-A4: Job CI wywołuje tę samą komendę H i nie zastępuje dotychczasowych kontroli.

**Testy i dowody.** H:crc, H, B4, L, D. Brak wymaganego testu sprzętowego dla zakresu tej karty; nie zalicza to odbioru późniejszej integracji.

Dozwolone artefakty: `tests/host/test_crc.c`, odpowiadające atrapy w `tests/host/stubs/crc/`, dane w `tests/fixtures/crc/` i wyłącznie rejestracja tego testu w `tests/host/Makefile`.  Raport: `docs/refactor/evidence/PR-003.md`; rejestr: tylko wiersz PR-003 w `STATUS_PL.md`.

**Merge, cofnięcie i przekazanie.** Scalić po review i spełnieniu kryteriów. Cofnięcie przez revert tej karty; nie zmienia formatu danych trwałych ani protokołu. Po merge wykonać kontrole SHA main zgodnie z kryteriami wspólnymi. Następna karta korzysta z tego rezultatu dopiero po statusie VERIFIED_MAIN.

