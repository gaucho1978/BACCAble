**PR-011 — Tryb silent zachowany przy otwarciu CAN**

Typ: **FIX**. Model: GPT-5.6 Sol / medium. Budżet produkcji: **250 linii**.

Wymagane dokumenty: [kontrakt](../EXECUTION_PL.md), [decyzje](../DECISIONS_PL.md), [akceptacja](../ACCEPTANCE_PL.md).

**Warunki wejścia:** [PR-003](PR-003.md). Bez dodatkowej bramki wejściowej; wymagany odbiór sprzętowy opisano poniżej.

**Problem i oczekiwany rezultat.** can_enable stosuje wybrany tryb zamiast zawsze NORMAL.

**Dozwolone pliki produkcyjne / narzędziowe.** `(nowy)` oznacza plik do utworzenia w tej karcie. Zmiana pliku wspólnego obejmuje tylko wskazaną odpowiedzialność.

- `firmware/ledsStripController/Core/Src/can.c`

**Dokładny zakres pracy.**

- Przechowywać żądany tryb niezależnie od stanu HAL.
- Zachować default NORMAL i odmowę zmiany przy ON_BUS.

**Poza zakresem.** Bitrate, autoretransmit, parser.

**Kryteria akceptacji.** Wszystkie warunki wspólne oraz:

- [ ] PR-011-A1: M1 przy OFF_BUS, potem O → HAL Init SILENT.
- [ ] PR-011-A2: M0/O i start bez M → NORMAL.
- [ ] PR-011-A3: M przy ON_BUS nie zmienia aktywnego/zapamiętanego trybu.
- [ ] PR-011-A4: Zamknięcie/otwarcie zachowuje ostatni poprawnie wybrany tryb.

**Testy i dowody.** H:pr011, pełne H, B4, L, D i M. Wymagany profil przed akceptacją aktywnej zmiany: **HW-USB**.

Dozwolone artefakty: `tests/host/test_pr011.c`, odpowiadające atrapy w `tests/host/stubs/pr011/`, dane w `tests/fixtures/pr011/` i wyłącznie rejestracja tego testu w `tests/host/Makefile`.  Raport: `docs/refactor/evidence/PR-011.md`; rejestr: tylko wiersz PR-011 w `STATUS_PL.md`.

**Merge, cofnięcie i przekazanie.** Scalić po review i spełnieniu kryteriów. Cofnięcie przez revert tej karty; nie zmienia formatu danych trwałych ani protokołu. Po merge wykonać kontrole SHA main zgodnie z kryteriami wspólnymi. Następna karta korzysta z tego rezultatu dopiero po statusie VERIFIED_MAIN.

