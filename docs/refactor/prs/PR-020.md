**PR-020 — Zakończenie impulsu QV po wyłączeniu**

Typ: **FIX**. Model: GPT-5.6 Sol / medium. Budżet produkcji: **250 linii**.

Wymagane dokumenty: [kontrakt](../EXECUTION_PL.md), [decyzje](../DECISIONS_PL.md), [akceptacja](../ACCEPTANCE_PL.md).

**Warunki wejścia:** [PR-003](PR-003.md). Bez dodatkowej bramki wejściowej; wymagany odbiór sprzętowy opisano poniżej.

**Problem i oczekiwany rezultat.** Wyłączenie ustawienia nie pozostawia aktywnego MOSFET-a.

**Dozwolone pliki produkcyjne / narzędziowe.** `(nowy)` oznacza plik do utworzenia w tej karcie. Zmiana pliku wspólnego obejmuje tylko wskazaną odpowiedzialność.

- `firmware/ledsStripController/Core/Src/functions_C1baccable.c`

**Dokładny zakres pracy.**

- Zakończenie aktywnego impulsu wykonać poza if(enabled).
- Wyłączenie funkcji kończy wyjście przy następnym ticku, kasuje oczekującą prośbę; bez impulsu przeciwnego.
- Przy enabled zachować piny i dotychczasową długość.

**Poza zakresem.** Nowe zachowanie klap i ekstrakcja wydechu.

**Kryteria akceptacji.** Wszystkie warunki wspólne oraz:

- [ ] PR-020-A1: Active + enabled=false → wyjścia nieaktywne na następnym ticku.
- [ ] PR-020-A2: Normalny impuls kończy się na bazowej granicy.
- [ ] PR-020-A3: Re-enable nie odtwarza starej prośby.
- [ ] PR-020-A4: Open/close i overflow timera nie generują dodatkowego impulsu.

**Testy i dowody.** H:pr020, pełne H, B4, L, D i M. Wymagany profil przed akceptacją aktywnej zmiany: **HW-FEATURE**.

Dozwolone artefakty: `tests/host/test_pr020.c`, odpowiadające atrapy w `tests/host/stubs/pr020/`, dane w `tests/fixtures/pr020/` i wyłącznie rejestracja tego testu w `tests/host/Makefile`.  Raport: `docs/refactor/evidence/PR-020.md`; rejestr: tylko wiersz PR-020 w `STATUS_PL.md`.

**Merge, cofnięcie i przekazanie.** Scalić po review i spełnieniu kryteriów. Cofnięcie przez revert tej karty; nie zmienia formatu danych trwałych ani protokołu. Po merge wykonać kontrole SHA main zgodnie z kryteriami wspólnymi. Następna karta korzysta z tego rezultatu dopiero po statusie VERIFIED_MAIN.

