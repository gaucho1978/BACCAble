**PR-037 — Pierwsza migracja pełnego snapshotu C1**

Typ: **INTEGRATE**. Model: GPT-5.6 Sol / medium. Budżet produkcji: **450 linii**.

Wymagane dokumenty: [kontrakt](../EXECUTION_PL.md), [decyzje](../DECISIONS_PL.md), [akceptacja](../ACCEPTANCE_PL.md).

**Warunki wejścia:** [PR-036](PR-036.md), [PR-021](PR-021.md), [PR-022](PR-022.md), [PR-028](PR-028.md). GATE-FLASH=PASS i urządzenie ma przygotowany wolumin D08. Bez tego tylko fixture/atrapy, bez aktywacji.

**Problem i oczekiwany rezultat.** C1 startuje z nowego snapshotu lub jednorazowo importuje pełne legacy.

**Dozwolone pliki produkcyjne / narzędziowe.** `(nowy)` oznacza plik do utworzenia w tej karcie. Zmiana pliku wspólnego obejmuje tylko wskazaną odpowiedzialność.

- `firmware/ledsStripController/Core/Src/storage_c1.c (nowy)`
- `firmware/ledsStripController/Core/Inc/storage_c1.h (nowy)`
- `firmware/ledsStripController/Core/Src/functions_C1baccable.c`
- `firmware/ledsStripController/Core/Src/setup_menu.c`

**Dokładny zakres pracy.**

- Importer czyta konfigurację, dwie statystyki i widoczność; pierwszy A/B zawiera wszystkie kategorie, mimo że nie wszystkie stare API już przełączono.
- Przy braku A/B walidować legacy, brakujące dane zastępować dotychczasowymi defaultami; migrować aktywny profil widoczności, drugi inicjować widocznymi stronami. Nie kasować legacy.
- Podłączyć load/save konfiguracji. Do PR-038 statystyki/widoczność pozostają autorytatywne w legacy: przed każdym save konfiguracji odświeżyć te pola snapshotu z legacy, aby ich nie cofnąć.

**Poza zakresem.** Nowe ustawienia, zmiana defaultów i przełączenie API statystyk/widoczności.

**Kryteria akceptacji.** Wszystkie warunki wspólne oraz:

- [ ] PR-037-A1: Fixtures pusty/poprawny/częściowo błędny legacy dają oczekiwany pełny snapshot.
- [ ] PR-037-A2: Power loss podczas pierwszej migracji pozostawia możliwość ponownego importu.
- [ ] PR-037-A3: Zapis ustawienia zachowuje aktualne statystyki i widoczność z legacy do PR-038.
- [ ] PR-037-A4: Kolejny boot z ważnym A/B nie nadpisuje nowszej konfiguracji starym legacy; błąd zapisu widoczny w UI.

**Testy i dowody.** H:pr037, pełne H, B4, L, D i M. Wymagany profil przed akceptacją aktywnej zmiany: **HW-FLASH + HW-UI**.

Dozwolone artefakty: `tests/host/test_pr037.c`, odpowiadające atrapy w `tests/host/stubs/pr037/`, dane w `tests/fixtures/pr037/` i wyłącznie rejestracja tego testu w `tests/host/Makefile`.  Raport: `docs/refactor/evidence/PR-037.md`; rejestr: tylko wiersz PR-037 w `STATUS_PL.md`.

**Merge, cofnięcie i przekazanie.** Downgrade odzyska ustawienia sprzed migracji; nowe są tylko w A/B. Wymagany backup i jawny opis tego skutku. Nie kasować A/B/legacy podczas revertu. Następna karta korzysta z tego rezultatu dopiero po statusie VERIFIED_MAIN.

