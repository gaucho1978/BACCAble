**PR-038 — Statystyki i widoczność C1 w jednym magazynie**

Typ: **INTEGRATE**. Model: GPT-5.6 Sol / medium. Budżet produkcji: **350 linii**.

Wymagane dokumenty: [kontrakt](../EXECUTION_PL.md), [decyzje](../DECISIONS_PL.md), [akceptacja](../ACCEPTANCE_PL.md).

**Warunki wejścia:** [PR-037](PR-037.md). Bez dodatkowej bramki wejściowej; wymagany odbiór sprzętowy opisano poniżej.

**Problem i oczekiwany rezultat.** Każda aktualizacja danych C1 zachowuje pozostałe pola snapshotu.

**Dozwolone pliki produkcyjne / narzędziowe.** `(nowy)` oznacza plik do utworzenia w tej karcie. Zmiana pliku wspólnego obejmuje tylko wskazaną odpowiedzialność.

- `firmware/ledsStripController/Core/Src/storage_c1.c`
- `firmware/ledsStripController/Core/Inc/storage_c1.h`
- `firmware/ledsStripController/Core/Src/functions_C1baccable.c`
- `firmware/ledsStripController/Core/Src/processingStandardMessage.c`

**Dokładny zakres pracy.**

- Przełączyć read/save/reset statystyk i widoczności na aktualizację wybranych TLV. Jednorazowo uzgodnić ostatnie legacy wartości z okresu PR-037 przed oznaczeniem migracji API jako zakończonej; marker użyć jako jawny klucz 0x03FF uint16=1.
- Nie pisać flash w dekoderze prędkości; zgłosić dirty/request do main. Zapis przyjmuje immutable snapshot i nie kasuje nowej zmiany powstałej podczas trwania operacji.
- Po zakończeniu migracji A/B jest autorytatywne dla wszystkich kategorii; zero zapisów legacy.

**Poza zakresem.** Zmiana algorytmu chronometru i zakresów rekordów.

**Kryteria akceptacji.** Wszystkie warunki wspólne oraz:

- [ ] PR-038-A1: Zapis nowego rekordu nie zmienia ustawień ani list widoczności.
- [ ] PR-038-A2: Zapis listy jednego profilu nie zmienia drugiego.
- [ ] PR-038-A3: Reset obu rekordów zachowuje resztę; error utrzymuje dirty.
- [ ] PR-038-A4: Aktualizacja między startem a końcem zapisu nie ginie; restart na każdym etapie migracji jest wznawialny.

**Testy i dowody.** H:pr038, pełne H, B4, L, D i M. Wymagany profil przed akceptacją aktywnej zmiany: **HW-FLASH + HW-UI**.

Dozwolone artefakty: `tests/host/test_pr038.c`, odpowiadające atrapy w `tests/host/stubs/pr038/`, dane w `tests/fixtures/pr038/` i wyłącznie rejestracja tego testu w `tests/host/Makefile`.  Raport: `docs/refactor/evidence/PR-038.md`; rejestr: tylko wiersz PR-038 w `STATUS_PL.md`.

**Merge, cofnięcie i przekazanie.** Revert do PR-037 wymaga świadomego wyboru źródła statystyk/widoczności z backupu; legacy nie zawiera zmian po PR-038. Bez automatycznego kopiowania wstecz. Następna karta korzysta z tego rezultatu dopiero po statusie VERIFIED_MAIN.

