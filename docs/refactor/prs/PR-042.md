**PR-042 — Niepodzielny obraz strony wysyłanej przez BH**

Typ: **FIX**. Model: GPT-5.6 Sol / medium. Budżet produkcji: **250 linii**.

Wymagane dokumenty: [kontrakt](../EXECUTION_PL.md), [decyzje](../DECISIONS_PL.md), [akceptacja](../ACCEPTANCE_PL.md).

**Warunki wejścia:** [PR-017](PR-017.md). Bez dodatkowej bramki wejściowej; wymagany odbiór sprzętowy opisano poniżej.

**Problem i oczekiwany rezultat.** Sześć ramek jednej strony nie miesza dwóch różnych tekstów.

**Dozwolone pliki produkcyjne / narzędziowe.** `(nowy)` oznacza plik do utworzenia w tej karcie. Zmiana pliku wspólnego obejmuje tylko wskazaną odpowiedzialność.

- `firmware/ledsStripController/Core/Src/functions_BHbaccable.c`
- `firmware/ledsStripController/Core/Src/uart.c`
- `firmware/ledsStripController/Core/Inc/globalVariables.h`
- `firmware/ledsStripController/Core/Src/globalVariables.c`
- `firmware/ledsStripController/Core/Src/processingStandardMessage.c`

**Dokładny zakres pracy.**

- Przed pierwszym fragmentem skopiować cały pending tekst do osobnego active snapshot; wszystkie kolejne fragmenty korzystają z active.
- Nowy tekst w trakcie wysyłki aktualizuje tylko pending; po ukończeniu active rozpocząć najnowszy kompletny pending.
- Zachować ilość fragmentów, 50 ms odstępu, nagłówki i kodowanie znaków; własność buforów opisać.
- Case BH 0x090 może restartować sekwencję: restart również rozpoczyna spójny snapshot od pierwszego fragmentu, nigdy kontynuacji z innym tekstem.

**Poza zakresem.** Nowy UART, szybsze odświeżanie i zmiana długości tekstu.

**Kryteria akceptacji.** Wszystkie warunki wspólne oraz:

- [ ] PR-042-A1: Tekst A, po trzecim fragmencie nadejście B → pozostałe fragmenty nadal A.
- [ ] PR-042-A2: Po końcu A następna cała strona jest B; przy B→C podczas A wysyłane najnowsze C.
- [ ] PR-042-A3: Idle i pusty tekst mają dotychczasową semantykę.
- [ ] PR-042-A4: Oba rozmiary ekranu bez naruszeń granic, raport dodatkowego RAM.

**Testy i dowody.** H:pr042, pełne H, B4, L, D i M. Wymagany profil przed akceptacją aktywnej zmiany: **HW-UI + HW-UART**.

Dozwolone artefakty: `tests/host/test_pr042.c`, odpowiadające atrapy w `tests/host/stubs/pr042/`, dane w `tests/fixtures/pr042/` i wyłącznie rejestracja tego testu w `tests/host/Makefile`.  Raport: `docs/refactor/evidence/PR-042.md`; rejestr: tylko wiersz PR-042 w `STATUS_PL.md`.

**Merge, cofnięcie i przekazanie.** Scalić po review i spełnieniu kryteriów. Cofnięcie przez revert tej karty; nie zmienia formatu danych trwałych ani protokołu. Po merge wykonać kontrole SHA main zgodnie z kryteriami wspólnymi. Następna karta korzysta z tego rezultatu dopiero po statusie VERIFIED_MAIN.
