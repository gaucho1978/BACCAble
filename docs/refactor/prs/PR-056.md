**PR-056 — Wydzielenie ograniczenia migania przebiegu**

Typ: **EXTRACT**. Model: GPT-5.6 Sol / medium. Budżet produkcji: **700 linii**.

Wymagane dokumenty: [kontrakt](../EXECUTION_PL.md), [decyzje](../DECISIONS_PL.md), [akceptacja](../ACCEPTANCE_PL.md).

**Warunki wejścia:** [PR-009](PR-009.md), [PR-017](PR-017.md). Bez dodatkowej bramki wejściowej; wymagany odbiór sprzętowy opisano poniżej.

**Problem i oczekiwany rezultat.** Jedna funkcja odpowiada za modyfikację wskazania przebiegu.

**Dozwolone pliki produkcyjne / narzędziowe.** `(nowy)` oznacza plik do utworzenia w tej karcie. Zmiana pliku wspólnego obejmuje tylko wskazaną odpowiedzialność.

- `firmware/ledsStripController/Core/Src/processingStandardMessage.c`
- `firmware/ledsStripController/Core/Src/functions_BHbaccable.c`
- `firmware/ledsStripController/Core/Src/uart.c`
- `firmware/ledsStripController/Core/Src/feature_odometer.c (nowy)`
- `firmware/ledsStripController/Core/Inc/feature_odometer.h (nowy)`
- `firmware/ledsStripController/Core/Src/globalVariables.c`
- `firmware/ledsStripController/Core/Inc/globalVariables.h`
- `firmware/ledsStripController/Core/Src/functions_C1baccable.c`
- `firmware/ledsStripController/Core/Src/setup_menu_entries.c`

**Dokładny zakres pracy.**

- Najpierw charakterystyka R na aktualnym main: konkretne wejściowe ramki/zdarzenia, wyjściowe bajty/GPIO, wartości stanu i wszystkie użyte progi; pochodzenie każdego fixture zapisane.
- Przenieść tylko maskowanie migania i obsługę BHcmdOdometerBlink*. Moduł zwraca kopię z dotychczasową maską, nie zmienia liczbowego przebiegu.
- Prywatny stan statyczny w feature_odometer.c; API D01, adaptery w dotychczasowych handlerach. Usunąć tylko przeniesione deklaracje extern; jeżeli inny caller potrzebuje odczytu, użyć getteru. Wymienione pliki wspólne obejmują wyłącznie tę funkcję.
- Zakres własności dotyczy stanu wykonawczego. Ustawienia wskazywane przez SetupParam pozostają u właściciela konfiguracji i są przekazywane jako wejście. Dodatkowe wymienione callery wolno zmienić wyłącznie na odczyt/wywołanie nowego API tej funkcji, bez zmiany ich pozostałej logiki.

**Poza zakresem.** Zmiana zachowania poza wcześniej scalonymi FIX; nowe usługi, piny, ramki, wartości domyślne, retry/timeout albo „poprawki przy okazji”. Nie przenosić innych funkcji z tych samych handlerów.

**Kryteria akceptacji.** Wszystkie warunki wspólne oraz:

- [ ] PR-056-A1: Włączona funkcja modyfikuje dokładnie dotychczasowe bity.
- [ ] PR-056-A2: Wyłączona przekazuje bazową ramkę bez modyfikacji.
- [ ] PR-056-A3: Wszystkie bajty poza maską pozostają bitowo identyczne dla kilku wartości wejścia.
- [ ] PR-056-A4: R przechodzi przed/po; stan ma jednego właściciela, moduł nie zawiera HAL, ISR nie wykonuje jego logiki. Testy obejmują granice czasowe i reset.

**Testy i dowody.** H:pr056, pełne H, B4, L, D, R i M. Wymagany profil przed akceptacją aktywnej zmiany: **HW-FEATURE**.

Dozwolone artefakty: `tests/host/test_pr056.c`, odpowiadające atrapy w `tests/host/stubs/pr056/`, dane w `tests/fixtures/pr056/` i wyłącznie rejestracja tego testu w `tests/host/Makefile`.  Raport: `docs/refactor/evidence/PR-056.md`; rejestr: tylko wiersz PR-056 w `STATUS_PL.md`.

**Merge, cofnięcie i przekazanie.** Scalić po review i spełnieniu kryteriów. Cofnięcie przez revert tej karty; nie zmienia formatu danych trwałych ani protokołu. Po merge wykonać kontrole SHA main zgodnie z kryteriami wspólnymi. Raport zawiera mapping stara zmienna/funkcja → nowe pole/API, listę zachowanych ograniczeń i fixture. Nie rozszerzać karty o ich naprawę.
