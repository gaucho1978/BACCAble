**PR-058 — Wydzielenie istniejącego kasowania DTC**

Typ: **EXTRACT**. Model: GPT-5.6 Sol / medium. Budżet produkcji: **700 linii**.

Wymagane dokumenty: [kontrakt](../EXECUTION_PL.md), [decyzje](../DECISIONS_PL.md), [akceptacja](../ACCEPTANCE_PL.md).

**Warunki wejścia:** [PR-017](PR-017.md), [PR-019](PR-019.md). Bez dodatkowej bramki wejściowej; wymagany odbiór sprzętowy opisano poniżej.

**Problem i oczekiwany rezultat.** Pętla kasowania błędów jest osobną funkcją, z zachowaną sekwencją odbiorców.

**Dozwolone pliki produkcyjne / narzędziowe.** `(nowy)` oznacza plik do utworzenia w tej karcie. Zmiana pliku wspólnego obejmuje tylko wskazaną odpowiedzialność.

- `firmware/ledsStripController/Core/Src/main.c`
- `firmware/ledsStripController/Core/Src/uart.c`
- `firmware/ledsStripController/Core/Src/feature_clear_faults.c (nowy)`
- `firmware/ledsStripController/Core/Inc/feature_clear_faults.h (nowy)`
- `firmware/ledsStripController/Core/Src/globalVariables.c`
- `firmware/ledsStripController/Core/Inc/globalVariables.h`
- `firmware/ledsStripController/Core/Src/functions_C1baccable.c`
- `firmware/ledsStripController/Core/Src/processingMessage0x000002FA.c`

**Dokładny zakres pracy.**

- Najpierw charakterystyka R na aktualnym main: konkretne wejściowe ramki/zdarzenia, wyjściowe bajty/GPIO, wartości stanu i wszystkie użyte progi; pochodzenie każdego fixture zapisane.
- Przenieść clearFaultsRequest i odstęp 25 ms z main do modułu; adapter wysyła obecne request ID i AllResetFaults. Zachować zakres iterowanych ECU i flagi CLEAR_FAULTS_ENABLED.
- Prywatny stan statyczny w feature_clear_faults.c; API D01, adaptery w dotychczasowych handlerach. Usunąć tylko przeniesione deklaracje extern; jeżeli inny caller potrzebuje odczytu, użyć getteru. Wymienione pliki wspólne obejmują wyłącznie tę funkcję.
- Zakres własności dotyczy stanu wykonawczego. Ustawienia wskazywane przez SetupParam pozostają u właściciela konfiguracji i są przekazywane jako wejście. Dodatkowe wymienione callery wolno zmienić wyłącznie na odczyt/wywołanie nowego API tej funkcji, bez zmiany ich pozostałej logiki.

**Poza zakresem.** Zmiana zachowania poza wcześniej scalonymi FIX; nowe usługi, piny, ramki, wartości domyślne, retry/timeout albo „poprawki przy okazji”. Nie przenosić innych funkcji z tych samych handlerów.

**Kryteria akceptacji.** Wszystkie warunki wspólne oraz:

- [ ] PR-058-A1: Żądanie generuje tę samą kolejność adresów i interwały co baza.
- [ ] PR-058-A2: W C1 AllResetFaults wysyłane zgodnie z bazowym początkiem sekwencji; C2/BH nie wysyłają go ponownie.
- [ ] PR-058-A3: Wyłączona zdolność blokuje funkcję; brak żądania daje zero CAN.
- [ ] PR-058-A4: R przechodzi przed/po; stan ma jednego właściciela, moduł nie zawiera HAL, ISR nie wykonuje jego logiki. Testy obejmują granice czasowe i reset.

**Testy i dowody.** H:pr058, pełne H, B4, L, D, R i M. Wymagany profil przed akceptacją aktywnej zmiany: **HW-DIAG + HW-FEATURE**.

Dozwolone artefakty: `tests/host/test_pr058.c`, odpowiadające atrapy w `tests/host/stubs/pr058/`, dane w `tests/fixtures/pr058/` i wyłącznie rejestracja tego testu w `tests/host/Makefile`.  Raport: `docs/refactor/evidence/PR-058.md`; rejestr: tylko wiersz PR-058 w `STATUS_PL.md`.

**Merge, cofnięcie i przekazanie.** Scalić po review i spełnieniu kryteriów. Cofnięcie przez revert tej karty; nie zmienia formatu danych trwałych ani protokołu. Po merge wykonać kontrole SHA main zgodnie z kryteriami wspólnymi. Raport zawiera mapping stara zmienna/funkcja → nowe pole/API, listę zachowanych ograniczeń i fixture. Nie rozszerzać karty o ich naprawę.
