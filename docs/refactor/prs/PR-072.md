**PR-072 — Jedna ścieżka kontroli build/CI/release**

Typ: **TOOL**. Model: GPT-5.6 Sol / medium. Budżet produkcji: **0 linii**.

Wymagane dokumenty: [kontrakt](../EXECUTION_PL.md), [decyzje](../DECISIONS_PL.md), [akceptacja](../ACCEPTANCE_PL.md).

**Warunki wejścia:** [PR-002](PR-002.md), [PR-003](PR-003.md), [PR-070](PR-070.md), [PR-071](PR-071.md). Bez dodatkowej bramki wejściowej; wymagany odbiór sprzętowy opisano poniżej.

**Problem i oczekiwany rezultat.** Wydanie zawiera wyłącznie sprawdzone obrazy jednego SHA i właściwych konfiguracji.

**Dozwolone pliki produkcyjne / narzędziowe.** `(nowy)` oznacza plik do utworzenia w tej karcie. Zmiana pliku wspólnego obejmuje tylko wskazaną odpowiedzialność.

- `.github/workflows/ci.yml`
- `.github/workflows/build.yml`
- `.github/workflows/build-unstable.yml`
- `.github/workflows/firmware-checks.yml (nowy)`
- `scripts/refactor/build.py`
- `scripts/refactor/build-matrix.json`

**Dokładny zakres pracy.**

- Współdzielony workflow buduje B4/BX z przypiętą wersją ARM GCC z manifestu i uruchamia H/L; osobne katalogi na wariant.
- Release stable/beta zależy od zakończenia kontroli tego samego SHA; artefakty przenosi z joba build zamiast kompilować ponownie inaczej.
- Usunąć placeholder ELF='EMPTY'; nieobsługiwany wariant jawnie pominięty z manifestem. Bez zmiany schematu nazw istniejących czterech artefaktów.

**Poza zakresem.** Publikowanie testowej release, zmiana tagowania/force-push i uprawnień repozytorium.

**Kryteria akceptacji.** Wszystkie warunki wspólne oraz:

- [ ] PR-072-A1: Suche sprawdzenie grafu workflow: release nie startuje po FAIL H/L/build.
- [ ] PR-072-A2: Manifest każdego artefaktu zawiera ten sam source SHA oraz własną rolę/flags.
- [ ] PR-072-A3: Stable/beta używają wspólnego builda; brak resztek wariantu w bibliotekach.
- [ ] PR-072-A4: Żaden plik tekstowy nie udaje ELF; workflow daje wymagane cztery obrazy standardowej konfiguracji.

**Testy i dowody.** B4, BX, H, L, D; walidacja YAML i test skryptów. Bez uruchamiania publikacji w ramach karty. Brak wymaganego testu sprzętowego dla zakresu tej karty; nie zalicza to odbioru późniejszej integracji.

Dozwolone artefakty: `tests/host/test_pr072.c`, odpowiadające atrapy w `tests/host/stubs/pr072/`, dane w `tests/fixtures/pr072/` i wyłącznie rejestracja tego testu w `tests/host/Makefile`.  Raport: `docs/refactor/evidence/PR-072.md`; rejestr: tylko wiersz PR-072 w `STATUS_PL.md`.

**Merge, cofnięcie i przekazanie.** Scalić po review i spełnieniu kryteriów. Cofnięcie przez revert tej karty; nie zmienia formatu danych trwałych ani protokołu. Po merge wykonać kontrole SHA main zgodnie z kryteriami wspólnymi. Następna karta korzysta z tego rezultatu dopiero po statusie VERIFIED_MAIN.

